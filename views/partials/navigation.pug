aside.navigation
  nav.navigation__wrapper
    // Metadata
    //.navigation__link__metadata
    //  p.navigation__link__metadata__title=meta.data.title
    //  p.navigation__link__metadata__description=meta.data.description

    // Nav links
    ul.navigation__list
      each item in navigation.data.list
        li.navigation__list__item
           a.navigation__list__link(href=link(item.link))=item.text
